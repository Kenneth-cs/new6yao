[3 tools called]

这是一个非常好的优化思路！既然数据有重叠，将它们合并不仅能精简界面，还能让"我的"页面内容更丰富、更有价值。

我为你制定了**合并方案**和**新的Tab布局**：

### 核心调整
1.  **合并功能**：将"成长"页面的**"最近决策记录"**搬家到"我的"页面，放在统计面板下方。
2.  **腾出位置**：原来的"成长"Tab位置空出来，刚好放入你想要的**"五行矩阵"**。

### 新的底部Tab布局 (保持5个)

1.  📚 **学习** (Learning)
2.  🧠 **思考** (Thinking) - *保留SWOT等工具箱*
3.  ✨ **摇卦** (Divination) - *六爻摇卦*
4.  🎯 **决策** (Matrix) - *五行决策矩阵 (核心功能上位)*
5.  👤 **我的** (Profile) - *包含个人信息+成长数据+历史记录*

---

### 第一步：升级"个人中心"页面

我将把"最近决策记录"列表添加到个人中心。

```swift:liuyao/ProfilePageView.swift
// ... existing code ...
struct ProfilePageView: View {
    @Environment(\.managedObjectContext) private var viewContext
    @ObservedObject private var statisticsService = StatisticsService.shared
    // 添加记录状态
    @State private var recentRecords: [DivinationRecord] = []
    
    @State private var showingCacheCleanup = false
// ... existing code ...
    var body: some View {
        // ... existing code ...
        profileContent
            .onAppear {
                statisticsService.loadStatistics(context: viewContext)
                fetchRecentRecords() // 加载记录
            }
    }
    
    // 获取最近记录
    private func fetchRecentRecords() {
        let request: NSFetchRequest<DivinationRecord> = DivinationRecord.fetchRequest()
        request.sortDescriptors = [NSSortDescriptor(keyPath: \DivinationRecord.createdAt, ascending: false)]
        request.fetchLimit = 3 // 只显示最近3条
        
        do {
            recentRecords = try viewContext.fetch(request)
        } catch {
            print("获取记录失败: \(error)")
        }
    }
    
    // 提取公共的内容视图
    private var profileContent: some View {
        ScrollView {
            VStack(spacing: 24) {
                // 用户信息区域
                UserInfoSection()
                
                // 订阅状态卡片
                SubscriptionStatusCard()
                
                // 统计面板
                StatisticsPanel(statisticsService: statisticsService)
                
                // 新增：最近决策记录 (从成长档案移过来)
                if !recentRecords.isEmpty {
                    RecentDecisionsSection(records: recentRecords)
                }
                
                // 应用管理
                AppManagementSection(
// ... existing code ...
```

并在文件末尾添加 `RecentDecisionsSection` 组件：

```swift:liuyao/ProfilePageView.swift
// ... existing code ...
// MARK: - 最近决策部分
struct RecentDecisionsSection: View {
    let records: [DivinationRecord]
    
    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            HStack {
                Image(systemName: "clock.arrow.circlepath")
                    .foregroundColor(.purple)
                    .font(.title3)
                Text("最近决策")
                    .font(.title3)
                    .fontWeight(.bold)
                
                Spacer()
                
                NavigationLink(destination: HistoryPageView()) {
                    HStack(spacing: 4) {
                        Text("查看全部")
                        Image(systemName: "chevron.right")
                    }
                    .font(.subheadline)
                    .foregroundColor(.secondary)
                }
            }
            
            VStack(spacing: 12) {
                ForEach(records, id: \.objectID) { record in
                    NavigationLink(destination: HistoryDetailView(record: record)) {
                        HStack {
                            VStack(alignment: .leading, spacing: 6) {
                                Text(record.question ?? "无标题")
                                    .font(.headline)
                                    .foregroundColor(.primary)
                                    .lineLimit(1)
                                
                                HStack {
                                    Text(record.type ?? "六爻预测")
                                        .font(.caption)
                                        .padding(.horizontal, 6)
                                        .padding(.vertical, 2)
                                        .background(Color.purple.opacity(0.1))
                                        .foregroundColor(.purple)
                                        .cornerRadius(4)
                                    
                                    if let date = record.createdAt {
                                        Text(date, style: .date)
                                            .font(.caption)
                                            .foregroundColor(.secondary)
                                    }
                                }
                            }
                            
                            Spacer()
                            
                            Image(systemName: "chevron.right")
                                .font(.caption)
                                .foregroundColor(.gray.opacity(0.5))
                        }
                        .padding()
                        .background(Color(.secondarySystemBackground))
                        .cornerRadius(12)
                    }
                    .buttonStyle(PlainButtonStyle())
                }
            }
        }
        .padding(20)
        .background(
            RoundedRectangle(cornerRadius: 16)
                .fill(Color(.systemBackground))
                .shadow(color: Color.primary.opacity(0.05), radius: 8, x: 0, y: 4)
        )
    }
}
```

### 第二步：更新底部 Tab 栏

修改 `MainTabView.swift`，应用新的 5 Tab 结构。

```swift:liuyao/MainTabView.swift
// ... existing code ...
        TabView(selection: $selectedTab) {
            // Tab 1: 学习中心 (保持不变)
            NavigationStack {
                LearningCenterView()
            }
            .tabItem {
                Label("学习", systemImage: "book.fill")
            }
            .tag(0)
            
            // Tab 2: 思维工具 (保持不变)
            NavigationStack {
                ThinkingToolsView()
            }
            .tabItem {
                Label("思考", systemImage: "brain.head.profile")
            }
            .tag(1)
            
            // Tab 3: 决策分析 (保持不变)
            NavigationStack {
                DecisionAnalysisView()
            }
            .tabItem {
                Label("决策", systemImage: "sparkles")
            }
            .tag(2)
            
            // Tab 4: 五行决策矩阵 (原成长档案位置)
            NavigationStack {
                DecisionMatrixView()
            }
            .tabItem {
                Label("矩阵", systemImage: "square.grid.3x3.fill")
            }
            .tag(3)
            
            // Tab 5: 个人中心 (内容已升级)
            NavigationStack {
                ProfileCenterView()
            }
            .tabItem {
                Label("我的", systemImage: "person.fill")
            }
            .tag(4)
        }
// ... existing code ...
```

**操作建议**：
1. 修改后，你可以把原来的 `GrowthProfileView.swift` 删除，或者留作备份。

这样调整后，界面结构更清晰，核心功能（矩阵）更突出，同时也没有丢失任何数据。