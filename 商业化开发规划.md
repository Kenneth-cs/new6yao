# 💎 "人生教练"App 商业化开发规划

## 📋 总体目标

实现**三层订阅模式**（免费版、专业版），通过功能限制和订阅引导，建立稳定的订阅收入。

---

## 🏗️ 开发架构设计

### 核心模块划分

```
商业化系统
├── 订阅管理层 (SubscriptionService)
│   ├── StoreKit 2 订阅购买
│   ├── 订阅状态管理
│   ├── 收据验证
│   └── 自动续订处理
│
├── 权限控制层 (PermissionManager)
│   ├── 使用次数限制
│   ├── 功能权限判断
│   ├── 本地计数器
│   └── 订阅权益校验
│
├── UI展示层
│   ├── 订阅引导页 (SubscriptionPromptView)
│   ├── 订阅详情页 (SubscriptionDetailView)
│   ├── 订阅管理页 (SubscriptionManagementView)
│   └── 达到限制提示 (LimitReachedView)
│
└── 数据持久层
    ├── UserDefaults (次数计数)
    ├── Keychain (订阅状态)
    └── Core Data (使用历史)
```

---

## 📦 文件结构规划

### 新建文件清单

```
liuyao/
├── Subscription/
│   ├── SubscriptionService.swift          # 订阅核心服务（StoreKit 2）
│   ├── SubscriptionModels.swift           # 订阅相关数据模型
│   ├── PermissionManager.swift            # 权限和次数管理
│   ├── SubscriptionDetailView.swift       # 订阅详情页（主订阅页面）
│   ├── SubscriptionPromptView.swift       # 订阅引导弹窗
│   ├── SubscriptionManagementView.swift   # 订阅管理页面
│   ├── LimitReachedView.swift             # 达到限制提示页
│   └── FeatureComparisonView.swift        # 功能对比表格
│
├── Extensions/
│   └── UserDefaults+Subscription.swift    # UserDefaults 扩展
│
└── Config/
    └── SubscriptionConfig.swift           # 订阅配置（产品ID、定价等）
```

### 需要修改的现有文件

```
✏️ DivinationPageView.swift           # 添加使用次数检查
✏️ CoinTossPageView.swift             # 添加分析前权限检查
✏️ SWOTAnalysisView.swift             # 添加SWOT工具次数限制
✏️ DecisionMatrixView.swift           # 添加决策矩阵次数限制
✏️ HistoryPageView.swift              # 添加记录数量限制
✏️ ProfilePageView.swift              # 添加订阅管理入口
✏️ MainTabView.swift                  # 添加全局订阅状态监听
✏️ DataModels.swift                   # 添加订阅相关Core Data模型
```

---

## 🎯 第一阶段：核心订阅系统（3-4天）

### Day 1: 订阅基础框架

#### 任务 1.1: 创建订阅数据模型
```swift
// SubscriptionModels.swift

enum SubscriptionTier: String {
    case free = "free"
    case pro = "pro_monthly"
    case proYearly = "pro_yearly"
    
    var displayName: String
    var price: String
    var features: [String]
}

struct SubscriptionStatus {
    let tier: SubscriptionTier
    let isActive: Bool
    let expirationDate: Date?
    let autoRenewing: Bool
}

struct UsageQuota {
    let dailyDivinationLimit: Int      // 每日问卦次数
    let monthlySWOTLimit: Int          // 每月SWOT次数
    let monthlyMatrixLimit: Int        // 每月决策矩阵次数
    let historyRecordsLimit: Int       // 历史记录保留数量
}
```

**预计时间**：2小时

#### 任务 1.2: 创建权限管理器
```swift
// PermissionManager.swift

class PermissionManager: ObservableObject {
    static let shared = PermissionManager()
    
    @Published var currentTier: SubscriptionTier = .free
    @Published var usageQuota: UsageQuota
    
    // 核心功能
    func canUseDivination() -> Bool
    func canUseSWOT() -> Bool
    func canUseMatrix() -> Bool
    func canSaveMoreRecords() -> Bool
    
    // 次数管理
    func incrementDivinationCount()
    func incrementSWOTCount()
    func incrementMatrixCount()
    
    func getDailyDivinationRemaining() -> Int
    func getMonthlySWOTRemaining() -> Int
    func getMonthlyMatrixRemaining() -> Int
    
    // 重置逻辑
    func resetDailyCounters()
    func resetMonthlyCounters()
}
```

**预计时间**：4小时

#### 任务 1.3: 创建订阅配置
```swift
// SubscriptionConfig.swift

struct SubscriptionConfig {
    // App Store 产品ID
    static let proMonthlyProductID = "com.renshengjiaoliankd.pro.monthly"
    static let proYearlyProductID = "com.renshengjiaoliankd.pro.yearly"
    
    // 免费版配额
    static let freeDailyDivination = 3
    static let freeMonthlySWOT = 10
    static let freeMonthlyMatrix = 10
    static let freeHistoryLimit = 10
    
    // 专业版配额（-1 表示无限）
    static let proDailyDivination = -1
    static let proMonthlySWOT = -1
    static let proMonthlyMatrix = -1
    static let proHistoryLimit = -1
}
```

**预计时间**：1小时

---

### Day 2: StoreKit 2 集成

#### 任务 2.1: 创建订阅服务
```swift
// SubscriptionService.swift

import StoreKit

@MainActor
class SubscriptionService: ObservableObject {
    static let shared = SubscriptionService()
    
    @Published var products: [Product] = []
    @Published var purchasedProductIDs: Set<String> = []
    @Published var subscriptionStatus: SubscriptionStatus?
    
    private var updateListenerTask: Task<Void, Error>?
    
    // 核心功能
    func loadProducts() async
    func purchase(_ product: Product) async throws -> Transaction?
    func checkSubscriptionStatus() async
    func restorePurchases() async
    
    // 监听订阅变化
    func observeTransactionUpdates()
}
```

**关键实现点**：
- 使用 StoreKit 2 的 `Product.products(for:)` 加载产品
- 使用 `Transaction.currentEntitlements` 检查订阅状态
- 监听 `Transaction.updates` 自动续订变化

**预计时间**：6小时

#### 任务 2.2: App Store Connect 配置
1. 登录 App Store Connect
2. 创建订阅组：`人生教练专业版`
3. 创建两个订阅产品：
   - 月订阅：`com.renshengjiaoliankd.pro.monthly` - ¥9.9/月
   - 年订阅：`com.renshengjiaoliankd.pro.yearly` - ¥99/年
4. 配置订阅描述和权益
5. 提交审核（可与App同步审核）

**预计时间**：2小时

---

### Day 3: UI界面开发（订阅详情页）

#### 任务 3.1: 订阅详情页主界面
```swift
// SubscriptionDetailView.swift

struct SubscriptionDetailView: View {
    @StateObject private var subscriptionService = SubscriptionService.shared
    @StateObject private var permissionManager = PermissionManager.shared
    
    var body: some View {
        ScrollView {
            VStack(spacing: 24) {
                // 1. 顶部标题区
                headerSection
                
                // 2. 订阅卡片选择
                subscriptionCardsSection
                
                // 3. 功能对比表
                FeatureComparisonView()
                
                // 4. 购买按钮
                purchaseButton
                
                // 5. 底部说明
                legalSection
            }
        }
    }
}
```

**设计要点**：
- 紫色主题色，与App整体风格一致
- 突出"推荐"标签（专业版年付）
- 清晰的价格展示和优惠说明
- 醒目的CTA按钮

**预计时间**：5小时

#### 任务 3.2: 功能对比表
```swift
// FeatureComparisonView.swift

struct FeatureComparisonView: View {
    let features = [
        ("决策分析", "每天3次", "无限次数"),
        ("SWOT分析", "每月10次", "无限次数"),
        ("决策矩阵", "每月10次", "无限次数"),
        ("历史记录", "保留10条", "无限保存"),
        ("深度分析", "❌", "✅"),
        ("导出功能", "❌", "✅"),
        ("趋势分析", "❌", "✅"),
    ]
}
```

**预计时间**：2小时

---

### Day 4: 订阅引导与提示

#### 任务 4.1: 订阅引导弹窗
```swift
// SubscriptionPromptView.swift

struct SubscriptionPromptView: View {
    @Binding var isPresented: Bool
    let trigger: PromptTrigger
    
    enum PromptTrigger {
        case dailyLimitReached
        case swotLimitReached
        case matrixLimitReached
        case historyLimitReached
        case featureLockedExport
        case featureLockedTrend
    }
    
    var body: some View {
        // 根据不同触发场景显示定制化文案
    }
}
```

**预计时间**：3小时

#### 任务 4.2: 达到限制提示页
```swift
// LimitReachedView.swift

struct LimitReachedView: View {
    let limitType: LimitType
    let remaining: Int?
    let resetTime: Date?
    
    enum LimitType {
        case dailyDivination
        case monthlySWOT
        case monthlyMatrix
    }
}
```

**预计时间**：2小时

---

## 🎯 第二阶段：功能集成与权限控制（2-3天）

### Day 5-6: 核心功能改造

#### 任务 5.1: 决策分析次数限制
**文件**：`DivinationPageView.swift`

```swift
// 在DivinationPageView中添加

@StateObject private var permissionManager = PermissionManager.shared
@State private var showSubscriptionPrompt = false

// 在"开始分析"按钮点击时检查
Button(action: {
    if permissionManager.canUseDivination() {
        // 执行原有逻辑
        permissionManager.incrementDivinationCount()
        navigateToCoinToss()
    } else {
        showSubscriptionPrompt = true
    }
}) {
    Text("开始分析")
}
.sheet(isPresented: $showSubscriptionPrompt) {
    SubscriptionPromptView(
        isPresented: $showSubscriptionPrompt,
        trigger: .dailyLimitReached
    )
}

// 添加剩余次数提示
if permissionManager.currentTier == .free {
    Text("今日还剩 \(permissionManager.getDailyDivinationRemaining()) 次免费分析")
        .font(.caption)
        .foregroundColor(.secondary)
}
```

**预计时间**：2小时

#### 任务 5.2: SWOT分析次数限制
**文件**：`SWOTAnalysisView.swift`

```swift
// 类似逻辑，在"开始分析"按钮添加权限检查

Button("开始分析") {
    if permissionManager.canUseSWOT() {
        permissionManager.incrementSWOTCount()
        startAnalysis()
    } else {
        showSubscriptionPrompt = true
    }
}

// 添加剩余次数显示
Text("本月还剩 \(permissionManager.getMonthlySWOTRemaining()) 次")
```

**预计时间**：1.5小时

#### 任务 5.3: 决策矩阵次数限制
**文件**：`DecisionMatrixView.swift`

（同SWOT，约1.5小时）

#### 任务 5.4: 历史记录数量限制
**文件**：`HistoryPageView.swift`

```swift
// 在获取历史记录时过滤
var filteredRecords: [DivinationRecord] {
    let allRecords = dataService.fetchAllRecords()
    
    if permissionManager.currentTier == .free {
        return Array(allRecords.prefix(SubscriptionConfig.freeHistoryLimit))
    }
    
    return allRecords
}

// 添加提示
if permissionManager.currentTier == .free && allRecords.count > 10 {
    VStack {
        Text("免费版仅显示最近10条记录")
        Button("升级查看全部 \(allRecords.count) 条记录") {
            showSubscriptionDetail = true
        }
    }
}
```

**预计时间**：2小时

---

### Day 7: 个人中心集成

#### 任务 7.1: 添加订阅管理入口
**文件**：`ProfilePageView.swift`

```swift
// 在"法律文档"之前添加"订阅管理"板块

Section {
    // 订阅状态卡片
    NavigationLink(destination: SubscriptionManagementView()) {
        HStack {
            VStack(alignment: .leading) {
                Text(subscriptionService.currentTierName)
                    .font(.headline)
                
                if subscriptionService.subscriptionStatus?.isActive == true {
                    Text("已订阅 · 到期时间：\(formattedExpiryDate)")
                        .font(.caption)
                        .foregroundColor(.secondary)
                } else {
                    Text("升级解锁完整功能")
                        .font(.caption)
                        .foregroundColor(.purple)
                }
            }
            
            Spacer()
            
            Image(systemName: "crown.fill")
                .foregroundColor(.yellow)
        }
    }
} header: {
    Text("订阅会员")
}
```

**预计时间**：2小时

#### 任务 7.2: 订阅管理页面
```swift
// SubscriptionManagementView.swift

struct SubscriptionManagementView: View {
    @StateObject private var subscriptionService = SubscriptionService.shared
    
    var body: some View {
        List {
            // 当前订阅状态
            subscriptionStatusSection
            
            // 升级/续订按钮
            if subscriptionService.currentTier == .free {
                upgradeSection
            }
            
            // 使用情况统计
            usageStatisticsSection
            
            // 订阅权益说明
            benefitsSection
            
            // 管理订阅（跳转到App Store）
            manageSection
        }
    }
}
```

**预计时间**：3小时

---

## 🎯 第三阶段：测试与优化（1-2天）

### Day 8: 沙盒测试

#### 任务 8.1: 创建沙盒测试账号
1. App Store Connect → 用户和访问 → 沙盒测试员
2. 创建2-3个测试账号
3. 在真机上退出iCloud，使用测试账号登录

**预计时间**：0.5小时

#### 任务 8.2: 测试场景
```
测试清单：
✅ 产品加载正常显示
✅ 月订阅购买流程
✅ 年订阅购买流程
✅ 购买后权限立即生效
✅ 恢复购买功能
✅ 订阅过期后权限回收
✅ 自动续订模拟
✅ 取消订阅流程
✅ 次数限制正确触发
✅ 订阅引导弹窗正常显示
✅ 跨设备订阅同步
```

**预计时间**：4小时

#### 任务 8.3: 边界情况处理
- 网络异常时的降级处理
- 收据验证失败的fallback
- 订阅状态不一致的修复逻辑
- 本地计数器异常重置

**预计时间**：2小时

---

### Day 9: UI/UX优化

#### 任务 9.1: 订阅引导时机优化
```
优化触发时机：
1. 首次使用到第3次时：轻提示"还剩1次免费机会"
2. 第4次点击时：显示订阅引导弹窗
3. 完成第5次分析后：在结果页底部显示升级卡片
4. 查看第11条历史记录时：显示"升级查看全部"
```

**预计时间**：2小时

#### 任务 9.2: 文案优化
- A/B测试不同订阅页标题
- 优化功能描述文案（强调价值而非限制）
- 添加社交证明（"已有XXX人升级"）
- 优化CTA按钮文案

**预计时间**：1小时

#### 任务 9.3: 性能优化
- 订阅状态缓存，减少网络请求
- 懒加载订阅页面资源
- 优化Core Data查询性能

**预计时间**：2小时

---

## 🎯 第四阶段：App Store准备与上线（1天）

### Day 10: 提审准备

#### 任务 10.1: App Store Connect配置
```
订阅信息配置：
- 订阅组名称：人生教练专业版
- 订阅组参考名称：Pro Subscription
- 产品1：月订阅
  - 产品ID：com.renshengjiaoliankd.pro.monthly
  - 参考名称：Professional Monthly
  - 订阅价格：¥9.9
  - 订阅时长：1个月
  - 免费试用：7天（可选）
  
- 产品2：年订阅
  - 产品ID：com.renshengjiaoliankd.pro.yearly
  - 参考名称：Professional Yearly
  - 订阅价格：¥99
  - 订阅时长：1年
  - 免费试用：7天（可选）
```

**预计时间**：1小时

#### 任务 10.2: 审核资料准备
```
审核说明：
本应用提供以下订阅服务：

专业版订阅（月付/年付）：
- 无限次AI决策分析
- 无限使用SWOT分析和决策矩阵工具
- 无限历史记录保存
- 深度分析报告
- 导出功能
- 成长趋势分析

免费版用户可体验基础功能，有使用次数限制。
订阅通过Apple App Store内购实现，遵循Apple订阅规则。

测试账号：
- 账号：[沙盒测试账号]
- 密码：[密码]
- 说明：可以测试订阅购买流程
```

**预计时间**：1小时

#### 任务 10.3: 截图更新
在现有截图基础上，补充订阅页面截图：
- 订阅详情页（iPhone 6.7英寸）
- 功能对比表（iPhone 6.7英寸）
- 订阅管理页（可选）

**预计时间**：1小时

#### 任务 10.4: 版本提交
- 更新版本号：1.3
- 更新版本说明：
  ```
  🎉 全新会员体系上线！
  
  ✨ 新增功能
  - 推出专业版订阅，解锁完整功能
  - 新增深度分析报告
  - 新增导出功能
  - 新增成长趋势分析
  
  💎 订阅权益
  - 无限次AI决策分析
  - 无限使用思维工具
  - 无限历史记录
  - 更多专业功能
  
  🆓 免费版说明
  - 每天3次免费决策分析
  - 每月10次思维工具使用
  - 保留最近10条记录
  
  📱 优化体验
  - 优化界面交互
  - 提升性能稳定性
  - 修复已知问题
  ```

**预计时间**：1小时

---

## 📊 开发进度总览

| 阶段 | 时间 | 任务 | 预计工时 | 优先级 |
|-----|------|------|---------|--------|
| **第一阶段** | Day 1-4 | 核心订阅系统 | 25小时 | 🔴 P0 |
| **第二阶段** | Day 5-7 | 功能集成 | 15小时 | 🔴 P0 |
| **第三阶段** | Day 8-9 | 测试优化 | 11小时 | 🟠 P1 |
| **第四阶段** | Day 10 | 上线准备 | 4小时 | 🟠 P1 |
| **总计** | 10天 | 全部任务 | **55小时** | - |

---

## 🛠️ 技术栈与依赖

### 核心技术
```
✅ SwiftUI - UI框架
✅ StoreKit 2 - 订阅管理
✅ Core Data - 数据持久化
✅ UserDefaults - 次数计数
✅ Combine - 响应式编程
```

### 第三方依赖
```
❌ 无需引入第三方库
```

---

## ⚠️ 风险与注意事项

### 技术风险
1. **StoreKit 2兼容性**
   - 最低支持iOS 15.0，需确认目标用户设备版本分布
   - 如有iOS 14用户，需降级使用StoreKit 1

2. **订阅状态同步延迟**
   - 购买后可能有几秒延迟才能生效
   - 需添加loading状态和重试机制

3. **沙盒测试限制**
   - 沙盒环境下订阅时长被压缩（1个月变5分钟）
   - 需在真实环境进一步验证

### 审核风险
1. **功能对比必须真实**
   - 确保免费版和付费版功能确实有差异
   - 不能故意降低免费版体验来逼迫用户付费

2. **订阅描述必须清晰**
   - 明确说明订阅价格、周期、自动续订规则
   - 提供清晰的取消订阅说明

3. **避免误导性营销**
   - 不要使用"限时优惠"等紧迫性话术（除非真实）
   - 不要在弹窗中过度干扰用户

---

## 💰 预期成本

### 开发成本
- 个人开发：0元（自己实现）
- 外包开发：约¥15,000 - ¥25,000

### 运营成本
- Apple开发者账号：¥688/年
- AI API成本：随用户增长而增加
- 无需额外服务器成本（StoreKit由Apple托管）

### Apple抽成
- 首年订阅：Apple抽成30%，到手70%
- 次年续订：Apple抽成15%，到手85%

---

## 🎯 关键成功指标（KPI）

### 转化漏斗
```
1. DAU（日活用户）→ 目标：500+
2. 触发订阅引导率 → 目标：60%
3. 查看订阅详情率 → 目标：30%
4. 订阅转化率 → 目标：2-5%
5. 订阅留存率（次月） → 目标：70%
```

### 收入指标
```
- 月度新增订阅数 → 目标：10+
- 月度订阅MRR → 目标：¥200+
- 年度订阅占比 → 目标：40%+
```

---

## 📝 后续优化方向

### v1.4 功能规划（2-3个月后）
1. **限时优惠系统**
   - 首月半价
   - 节日特惠
   - 邀请好友送会员

2. **终身版买断**
   - 一次性¥699
   - 适合高忠诚度用户

3. **会员专属功能**
   - 月度成长报告
   - 更高级的AI模型
   - 专属客服通道

4. **推荐奖励机制**
   - 邀请好友奖励额外次数
   - 双方都获得7天试用

---

## ✅ 下一步行动

### 立即开始（今天）
1. ✅ 阅读并确认开发规划
2. 🔄 创建订阅相关文件夹结构
3. 🔄 实现`SubscriptionModels.swift`
4. 🔄 实现`PermissionManager.swift`
5. 🔄 实现`SubscriptionConfig.swift`

### 本周完成
- Day 1-4：完成订阅核心系统
- App Store Connect配置订阅产品

### 下周完成
- Day 5-7：功能集成与权限控制
- Day 8-9：测试与优化
- Day 10：提审准备

---

## 💡 重要提醒

1. **订阅产品ID一旦创建不可修改**
   - 请在App Store Connect中仔细确认产品ID
   - 建议格式：`com.[bundle-id].pro.monthly/yearly`

2. **必须在真机测试订阅功能**
   - 模拟器无法测试StoreKit
   - 需要使用沙盒测试账号

3. **订阅审核通常需要额外时间**
   - 首次包含订阅的版本可能审核时间更长
   - 建议预留3-5天审核时间

4. **务必添加恢复购买功能**
   - Apple要求所有订阅App必须提供恢复购买按钮
   - 通常放在订阅页面或个人中心

---

## 📞 需要帮助？

如果开发过程中遇到问题，优先查阅：
1. Apple StoreKit 2 官方文档
2. WWDC 2021/2022 订阅相关Session
3. 本规划文档的"风险与注意事项"章节

---

**规划制定时间**：2025-12-01  
**预计开发完成时间**：2025-12-11  
**预计提审上线时间**：2025-12-16

---

🚀 **准备好开始了吗？让我们一步步实现商业化目标！**

